sharding:
  shards:
    - url: jdbc:postgresql://localhost:5432/urlshortener
      username: app
      password: app
    - url: jdbc:postgresql://localhost:5433/urlshortener
      username: app
      password: app

spring:
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

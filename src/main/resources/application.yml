sharding:
  shards:
    - writeUrl: jdbc:postgresql://localhost:5432/urlshortener
      readUrl: jdbc:postgresql://localhost:5434/urlshortener
      username: app
      password: app
    - writeUrl: jdbc:postgresql://localhost:5433/urlshortener
      readUrl: jdbc:postgresql://localhost:5435/urlshortener
      username: app
      password: app

spring:
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080
